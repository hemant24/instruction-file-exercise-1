{
  "name": "task-manager-api",
  "version": "1.0.0",
  "description": "Simple Express Task Manager API — AI instruction file exercise",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "NODE_ENV=test jest --forceExit",
    "test:routes": "NODE_ENV=test jest --testPathPattern=tests/routes --forceExit",
    "test:services": "NODE_ENV=test jest --testPathPattern=tests/services --forceExit",
    "lint": "eslint src/ tests/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
